# Production / Netlify — set these in Netlify → Site settings → Environment variables
# Then trigger a new deploy so the build uses them.

# Backend API URL (from Render or your backend host). No trailing slash.
REACT_APP_API_URL=https://bili-api.onrender.com

# Optional: WebSocket URL for Radar. If unset, derived from REACT_APP_API_URL (https → wss).
# REACT_APP_WS_URL=wss://bili-api.onrender.com

# Google Maps (same key as in backend; restrict by HTTP referrer in Google Cloud)
REACT_APP_GOOGLE_MAPS_API_KEY=your_maps_key
