# Deploy BILI frontend to Netlify (drag build folder or connect repo)
[build]
  command = "npm run build"
  publish = "build"

# SPA: all routes serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
