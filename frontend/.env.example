# BILI Frontend - Environment (optional)
# Use for production build when API is on a different host.

# Live API URL (no trailing slash). If empty, same-origin is used (e.g. proxy in dev).
# Example for Netlify + separate backend:
# REACT_APP_API_URL=https://your-bili-api.herokuapp.com
# REACT_APP_API_URL=https://api.yourdomain.com

# Google Maps JavaScript API key (map display only; no Places API).
# Restrict this key to Maps JavaScript API + HTTP referrers in Google Cloud Console.
# REACT_APP_GOOGLE_MAPS_API_KEY=your_maps_js_api_key
